var log = console.log,
dataServer = 'http://localhost',
w = window,
d = document,
e = d.documentElement,
g = d.getElementsByTagName('body')[0],
cwidth = w.innerWidth || e.clientWidth || g.clientWidth,
cheight = w.innerHeight|| e.clientHeight|| g.clientHeight,
margin = 35,
zoomMult = 1,
candleWidth = 3,
candleSpacing = candleWidth * 0.75,
histDepth = Math.ceil( ((cwidth-(2*margin)) / (candleWidth*1.75)) + (candleWidth*1.75) );